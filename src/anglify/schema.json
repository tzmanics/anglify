{
  "$schema": "http://json-schema.org/schema",
  "id": "AnglifyNgAdd",
  "title": "ng add schematic",
  "type": "object",
  "description": "Creates a config file and gitignores it 🔒",
  "properties": {
    "api": {
      "type": "string",
      "description": "The user's API ID.",
      "x-prompt": "What is your API ID?"
    },
    "token": {
      "type": "string",
      "description": "The user's Personal Access Token.",
      "x-prompt": "What is your Personal Access Token?"
    },
    "project": {
      "type": "string",
      "description": "The project name.",
      "x-prompt": "What is your project?"
    }
  },
  "required": ["api", "token", "project"]
}
